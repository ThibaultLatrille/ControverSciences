<% provide(:title, "Edition du profil") %>

<h2> Modifier un commentaire existant </h2>

La référence :

<%= link_to Reference.find(session[:reference_id]).title,  Reference.find(session[:reference_id]) %> <br>
La timeline :
<%= link_to Timeline.find(session[:timeline_id]).name,  Timeline.find(session[:timeline_id]) %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">

    <textarea class="form-control" rows="8" disabled> <%= @content %> </textarea>

    <%= form_for(@comment) do |f| %>
        <%= render 'shared/error_messages', object: f.object %>

        <%= f.label "Le champ" %>
        <%= f.select :field, [["L'éxperience", 1], ["Les résultats", 2],["Les limites", 3],["Quel rapport", 4],["Remarques", 5] ], {}, {class: 'staticfieldstore form-control', disabled: 'disabled'} %>

        <%= f.label "Votre nouveau commentaire" %>
        <%= f.text_area :content, class: 'form-control', rows: 16 %>

        <%= f.submit "Modifier le commentaire", class: "btn btn-primary" %>
    <% end %>

  </div>
</div>