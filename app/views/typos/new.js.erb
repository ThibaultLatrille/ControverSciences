$("body").append("<%= escape_javascript(render partial: 'typos/modal',
        locals: { typo: @typo } ) %>");
$('#newtypo').modal('show');
$(document).ready(function() {
    $('textarea', "#newtypo").autosize();
});