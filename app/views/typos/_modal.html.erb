<div class="modal fade" id="newtypo" tabindex="-1" role="dialog">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <br/>
      </div>
      <div class="modal-body clearfix">
        <div class="panel panel-default">
          <div class="panel-heading">
            <p>
              Si vous avez déniché un bug, une faute, ou si vous avez des remarques sur le site,
              ne soyez pas égoïste, faites le nous savoir, cette page est là pour ça ;-)
            </p>
          </div>
        </div>
        <%= form_for(typo, url: typos_path, remote: true,
                     html:      {role: :form, 'data-model' => 'issue'}) do |f| %>
            <%= f.hidden_field :summary_id, value: typo.summary_id %>
            <%= f.hidden_field :comment_id, value: typo.comment_id %>
            <%= f.hidden_field :field, value: typo.field %>
            <%= f.label :content, "Correction", class: 'control-label' %>
            <br>
            <%= f.text_area :content, class: 'form-control textarea' %>
            <div class="actions pull-right">
              <%= f.button "Envoyer &nbsp <span class=\"glyphicon glyphicon-send\" aria-hidden=\"true\"></span>".html_safe,
                           class: 'btn btn-success',
                           data:  {disable_with: "<span class=\"glyphicon glyphicon-send\" aria-hidden=\"true\"></span> &nbsp Envoi "} %>
            </div>
        <% end %>
      </div>
    </div>
  </div>
</div>