<div class="modal fade" id="newtypo" tabindex="-1" role="dialog">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <br/>
      </div>
      <div class="modal-body clearfix">
        <div class="panel panel-info">
          <div class="panel-heading">
            <% if typo.target_user_id == current_user.id %>
                <b>
                  Si une faute de français dans votre propre contribution vous a brulé les yeux, vous pouvez la corriger.
                </b>
            <% else %>
                <b>
                  Si une faute de français vous a brulé les yeux, vous pouvez proposer une correction ici à l'auteur.
                  Celle-ci lui sera envoyée pour être soumise à validation.
                </b>
            <% end %>
          </div>
        </div>
        <%= form_for(typo, url: typos_path, remote: true,
                     html:      {role: :form, 'data-model' => 'issue'}) do |f| %>
            <%= f.hidden_field :summary_id, value: typo.summary_id %>
            <%= f.hidden_field :comment_id, value: typo.comment_id %>
            <%= f.hidden_field :frame_id, value: typo.frame_id %>
            <%= f.hidden_field :field, value: typo.field %>
            <%= f.label :content, "Correction", class: 'control-label' %>
            <br>
            <%= f.text_area :content, class: 'form-control textarea' %>
            <div class="actions pull-right">
              <br/>
              <%= f.button "Envoyer la corretion &nbsp <span class=\"bigger icon-bug-#{rand(18)}\" aria-hidden=\"true\"></span>".html_safe,
                           class: 'btn btn-success',
                           data:  {disable_with: "<span class=\"glyphicon glyphicon-send\" aria-hidden=\"true\"></span> &nbsp Envoi "} %>
            </div>
        <% end %>
      </div>
    </div>
  </div>
</div>